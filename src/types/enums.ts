export enum Status {
    PENDING = "pending",
    APPROVED = "approved",
    REJECTED = "rejected",
    EXPIRED = "expired"
  }
  
  export enum Roles {
    ADMIN = "admin",
    USER = "user",
    DESIGNER = "designer"
  }

