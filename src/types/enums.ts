export enum Status {
    PENDING = "pending",
    APPROVED = "approved",
    REJECTED = "rejected",
    EXPIRED = "expired",
    CANCELLED = "cancelled"
  }
  
  export enum Roles {
    ADMIN = "admin",
    USER = "user",
    DESIGNER = "designer"
  }

